# Awes Mubarak's dotfiles

Install using:

```bash
curl "https://raw.githubusercontent.com/awesmubarak/dotfiles/main/.install_dotfiles.sh" > tmp_install.sh
chmod+x tmp_install.sh
./tmp_install.sh
rm tmp_install.sh
source .zshrc # if in zsh, otherwise run zsh
```
